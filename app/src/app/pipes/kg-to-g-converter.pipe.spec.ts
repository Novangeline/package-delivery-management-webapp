import { KgToGConverterPipe } from './kg-to-g-converter.pipe';

describe('KgToGConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new KgToGConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
